<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.termproject2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Family_Tree"></activity>
        <activity android:name=".Family_Quest" />
        <activity android:name=".Family_Login" />
        <activity android:name=".Family_Gallery" />
        <activity android:name=".Family_Date" />
        <activity android:name=".Family_Calendar" />
        <activity android:name=".Signup" />
        <activity android:name=".Mainpage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>